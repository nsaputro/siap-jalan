name: "SiapJalan (dev)"
version: "0.1.3b2"
slug: siap_jalan_dev
description: >-
  ⚠️ Development build — published manually via the Pre-release workflow.
  Smart travel packing assistant with AI suggestions and activity-based templates.
url: https://github.com/nsaputro/siap-jalan
# Dev images published via GitHub Actions → Pre-release workflow (manual trigger)
image: "ghcr.io/nsaputro/siap-jalan/{arch}-siap_jalan_dev"
arch:
  - amd64
  - aarch64
init: false
ingress: true
ingress_port: 8100
panel_icon: mdi:bag-suitcase-outline
panel_title: "SiapJalan (dev)"
options:
  anthropic_api_key: ""
  log_level: debug
schema:
  anthropic_api_key: str
  log_level: list(debug|info|warning|error)
